# Backend API Base URL
# Local Development (uses Vite proxy to http://localhost:3000):
VITE_API_URL=/api

# Production (uncomment and replace with your deployed backend domain):
# VITE_API_URL=https://your-backend-domain.com/api
